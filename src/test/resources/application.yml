# MyBatis
mybatis:
  mapper-locations:
    - classpath:mapper/*.xml
  configuration:
    use-generated-keys: true
    call-setters-on-nulls: true
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/dl?useSSL=false&characterEncoding=utf8&allowMultiQueries=true
    username: root
    password: root
    # DruidDataSource
    type: com.alibaba.druid.pool.DruidDataSource